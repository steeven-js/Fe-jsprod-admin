// ----------------------------------------------------------------------

import { Box, Typography } from '@mui/material';

export function PolicyView() {
  // const pageProgress = useScrollProgress();

  return (
    <Box>
      <Typography>PolicyView</Typography>
    </Box>
  );
}
