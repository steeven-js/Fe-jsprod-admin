// ----------------------------------------------------------------------

import { Box, Typography } from '@mui/material';

export function PlantsView() {
  // const pageProgress = useScrollProgress();

  return (
    <Box>
      <Typography>PlantsView</Typography>
    </Box>
  );
}
